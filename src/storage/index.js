// store.js

import { createStore } from 'vuex';

const store = createStore({
    state: {
        // Стан вашого додатку
    },
    mutations: {
        // Мутації для зміни стану
    },
    actions: {
        // Дії, які можуть викликати мутації
    },
    getters: {
        // Геттери для отримання даних зі стану
    },
    modules: {
        // Модулі, якщо використовуєте їх
    },
});

export default store;
